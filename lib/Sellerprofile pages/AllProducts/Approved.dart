import 'package:flutter/material.dart';

class ApprovedProducts extends StatelessWidget {
  const ApprovedProducts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('Approved'));
  }
}
